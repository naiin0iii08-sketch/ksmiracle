services:
  - type: web
    name: ksmiracle-web
    runtime: docker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ksmiracle-db
          property: connectionString

databases:
  - name: ksmiracle-db
    plan: free
